@namespace RemoteMaster.Client.Components

<!--<BitModal @ref="_modalRef" @bind-IsOpen="IsOpen" IsBlocking="true">
    <div class="modal-header">
        <span>New Folder</span>
        <BitIconButton OnClick=@(()=> IsOpen = false) IconName="@BitIconName.ChromeClose" Title="Close" />
    </div>
    <div class="modal-body space-y-2">
        <BitTextField Label="Name" IsRequired="true" MaxLength="10" IsUnderlined="true" />
        <BitButton>
            Create
        </BitButton>
    </div>
</BitModal>-->

<style>
    .modal-header {
        display: flex;
        align-items: center;
        font-size: 24px;
        font-weight: 600;
        border-top: 4px solid #0054C6;
        justify-content: space-between;
        padding: 12px 12px 14px 24px;
    }

    .modal-body {
        padding: 0 24px 24px;
        overflow-y: hidden;
        line-height: 20px;
        max-width: 960px;
    }
</style>