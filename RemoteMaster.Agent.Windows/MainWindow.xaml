<Window x:Class="RemoteMaster.Agent.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RemoteMaster.Agent"
        Title="RemoteMaster Client" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Window.Resources>
        <Style x:Key="CustomButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="BorderBrush" Value="#2196F3"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsEnabled" Value="True"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" Value="#1976D2"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#f5f5f5">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Configuration" Margin="10" Background="#ffffff" BorderBrush="#ddd">
            <StackPanel Margin="5">
                <TextBlock x:Name="ServiceStatusTextBlock" Text="Service Status: Unknown" Margin="0,10,0,0" FontSize="15" Foreground="#333"/>
                <TextBlock x:Name="ServerAddressTextBlock" Text="Server Address: 192.168.1.1" Margin="0,10,0,0" FontSize="15" Foreground="#333"/>
                <TextBlock x:Name="GroupTextBlock" Text="PC Group: Group1" Margin="0,10,0,0" FontSize="15" Foreground="#333"/>
            </StackPanel>
        </GroupBox>

        <DockPanel Grid.Row="1" Margin="10,10,10,10">
            <Button Style="{StaticResource CustomButton}" DockPanel.Dock="Left" Content="Install/Update" Width="130" Height="35" Margin="5,0,5,0" Click="InstallUpdateButton_Click" />
            <Button Style="{StaticResource CustomButton}" x:Name="UninstallButton" DockPanel.Dock="Left" Content="Uninstall" Width="130" Height="35" Margin="5,0,5,0" Click="UninstallButton_Click" Background="#F44336" BorderBrush="#F44336" IsEnabled="False"/>
        </DockPanel>
    </Grid>
</Window>
