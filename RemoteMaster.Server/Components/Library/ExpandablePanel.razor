<div class="@GetPanelClasses()">
    <h3 class="@GetHeaderClasses()" @onclick="Toggle" aria-expanded="@IsExpanded" aria-disabled="@IsDisabled" aria-label="@AriaLabel">
        @if (HeaderContent != null)
        {
            @HeaderContent
        }
        else
        {
            <span class="@GetIconClasses()" style="@GetTransitionStyles()">
                @Icon
            </span>
            @Title
        }
    </h3>

    @if (IsExpanded)
    {
        <div class="@GetContentContainerClasses()" style="@GetTransitionStyles()">
            @ChildContent
        </div>
    }
</div>
