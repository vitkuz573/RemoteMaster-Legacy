@using RemoteMaster.Server.Components.Library.Abstractions

@inject IDialogWindowService DialogService

@foreach (var dialogRef in _dialogs)
{
    <CascadingValue Value="dialogRef.Instance">
        @dialogRef.RenderFragment
    </CascadingValue>
}
