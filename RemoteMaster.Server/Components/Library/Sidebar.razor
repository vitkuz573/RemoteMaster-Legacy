<div style="transition-duration:@(AnimationDurationMs)ms; right:@(_isSidebarOpen ? "0" : $"-{WidthPx}px"); width:@($"{WidthPx}px")" class="@(CustomClass) fixed top-0 h-full w-64 p-5 shadow-lg transition-all duration-500 ease-out">
    <h2 class="mb-4 text-lg font-bold">@Title</h2>
    @ChildContent
    <div class="absolute inset-y-1/2 -left-5 flex h-10 w-5 cursor-pointer items-center justify-center rounded-bl-full rounded-tl-full bg-gray-800 transition-opacity duration-300 @(!_isSidebarOpen ? "opacity-50" : "opacity-100")" @onclick="ToggleSidebar">
        <span class="material-icons text-white">
            @(_isSidebarOpen ? IconOpen : IconClosed)
        </span>
    </div>
</div>
